<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Configuration
  ================================
  Based on research (February 2026):
  - TLS 1.3 recommended for health apps
  - Certificate pinning for production
  - Debug allows localhost for development
-->
<network-security-config>
    <!-- Production domain with certificate pinning -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.sleepcore.ru</domain>
        <pin-set>
            <!-- TODO: Add real certificate pins before production -->
            <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>
        </pin-set>
    </domain-config>

    <!-- Debug configuration allows localhost -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>

    <!-- Base config - no cleartext allowed -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
