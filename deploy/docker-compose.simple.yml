name: sleepcore-monitoring
services:
  grafana:
    image: grafana/grafana:10.2.0
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=SleepCore2025!
    volumes:
      - grafana-data:/var/lib/grafana
volumes:
  grafana-data:
